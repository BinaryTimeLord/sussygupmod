# Allows you to create your own dynamic keys
# to be called in localization.
# defined_text -> this is it, we're defining the text
# text -> a discrete entry that can be picked to display in loc.
# trigger -> determines if a text entry will be picked or not.
# (The triggers need to be valid for the scope the key is called in
# (eg Root or From.From).)
# localization_key -> points to the localization key
# that'll be used if trigger passes

defined_text = {
	name = GetSuperEventsBody
	text = {
		trigger = {
			has_global_flag = GUP_SuperEvents_Test
		}
		localization_key = GUP_SuperEvents_Test_Body
	}
	text = {
		trigger = {
			has_global_flag = GUP_63rd_Tournament
		}
		localization_key = GUP_SuperEvents_63rd_Body
	}
	text = {
		trigger = {
			has_country_flag = GUP_Darjeeling_Event
		}
		localization_key = GUP_SuperEvents_Darjeeling_Body
	}
	text = {
		trigger = {
			has_country_flag = GUP_Rosehip_Event
		}
		localization_key = GUP_SuperEvents_Rosehip_Body
	}
	text = {
		trigger = {
			has_global_flag = GUP_Pravda_Fascist_CW
		}
		localization_key = GUP_SuperEvents_Pravda_Fascist_CW_Body
	}
	text = {
		trigger = {
			has_global_flag = GUP_Pravda_Leftist_CW
		}
		localization_key = GUP_SuperEvents_Pravda_Leftist_CW_Body
	}
	text = {
		trigger = {
			has_global_flag = GUP_Pravda_Rightist_CW
		}
		localization_key = GUP_SuperEvents_Pravda_Rightist_CW_Body
	}
}